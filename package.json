{
  "name": "sportx-monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "SportX — Unified Monorepo (Frontend + Express API Backend)",
  "scripts": {
    "dev": "npx concurrently \"npm --prefix backend/functions run dev\" \"npm --prefix frontend run dev\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend/functions run dev",
    "typecheck": "npm --prefix frontend run typecheck && npm --prefix backend/functions run build",
    "test": "npm --prefix backend/functions test && npm --prefix frontend test",
    "test:backend": "npm --prefix backend/functions run test:backend",
    "test:frontend": "npm --prefix frontend test",
    "test:p2": "npm --prefix backend/functions run test:p2 && npm --prefix frontend run test:p2",
    "test:unit": "npm --prefix backend/functions test",
    "test:emulator": "npm --prefix backend/functions run test:emulator",
    "ci": "npm run typecheck && npm run test",
    "build": "cd backend/functions && npm install --include=dev && npm run build && cd ../../frontend && npm install --include=dev && npm run build",
    "vercel-build": "cd backend/functions && npm install --include=dev && npm run build && cd ../../frontend && npm install --include=dev && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
