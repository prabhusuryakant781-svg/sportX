{
  "name": "sportx-monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "SportX — Unified Monorepo (Frontend + Express API Backend)",
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd backend/functions && npm install && cd ../../frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/morgan": "^1.9.9",
    "@types/node": "^20.14.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
